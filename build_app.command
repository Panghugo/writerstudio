#!/bin/bash
cd "$(dirname "$0")"
echo "The legacy desktop app build has been retired."
echo "Writer Studio now runs as a local web app."
echo ""
echo "Use ./Start_Web.command to start it, or ./scripts/install_autostart.sh for login autostart."
read -p "Press Enter to close..."
